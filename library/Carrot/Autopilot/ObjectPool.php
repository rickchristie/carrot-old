<?php

namespace Carrot\Autopilot;

/**
 * Handles the saving of instantiated objects, this class will
 * refuse to save transient lifecycle objects, resulting in
 * the Container to always instantiate a new one everytime it
 * is needed.
 *
 */
class ObjectPool
{
    
}